<% provide(:title, 'All projects') %>
<h1>All Projects</h1>
<%= will_paginate %>

<ul class="Projects">
  <% @projects.each do |project| %>
    <%= render project%>
  <% end %>
</ul>

<%= will_paginate %>

<span> <%= link_to "Create new project", new_project_path %></span>